# Dynamic Row Ledger

動的行は、固定できる安定列をpropertiesへ昇格し、対象マスタ/CSV/外部カタログで増える列だけ互換境界として許容します。
dynamicRowsWithoutReason と、理由なしproperties=0を品質ゲートにします。

| path | classification | title | properties | reason | schema decision |
|---|---|---|---:|---|---|
| `var/json_schema/get-admin-authority-role.json:authorityOptions.items` | dynamic-master-or-csv-row | 権限選択肢 | 2 | 配列要素はマスタ/CSV/option mapの動的行であり、列集合が対象種別により変わるため固定shape化しない。既知propertyは契約し、追加列は該当サービス境界で扱う。 | 安定列はpropertiesへ昇格し、対象マスタ/CSV/外部カタログで増える列だけ互換境界として許容する。 |
| `var/json_schema/get-admin-authority-role.json:rules.items` | dynamic-master-or-csv-row | 権限ルール | 2 | 配列要素はマスタ/CSV/option mapの動的行であり、列集合が対象種別により変わるため固定shape化しない。既知propertyは契約し、追加列は該当サービス境界で扱う。 | 安定列はpropertiesへ昇格し、対象マスタ/CSV/外部カタログで増える列だけ互換境界として許容する。 |
| `var/json_schema/get-admin-index.json:recommendedPlugins.items` | external-catalog-row | 推奨プラグイン | 4 | 配列要素はマスタ/CSV/option mapの動的行であり、列集合が対象種別により変わるため固定shape化しない。既知propertyは契約し、追加列は該当サービス境界で扱う。 | 安定列はpropertiesへ昇格し、対象マスタ/CSV/外部カタログで増える列だけ互換境界として許容する。 |
| `var/json_schema/get-admin-index.json:orderStatuses.items` | status-summary-row | 注文ステータス | 3 | 配列要素はマスタ/CSV/option mapの動的行であり、列集合が対象種別により変わるため固定shape化しない。既知propertyは契約し、追加列は該当サービス境界で扱う。 | 安定列はpropertiesへ昇格し、対象マスタ/CSV/外部カタログで増える列だけ互換境界として許容する。 |
| `var/json_schema/get-admin-master-data.json:masterTypes.items` | dynamic-master-or-csv-row | マスタ種別 | 3 | 配列要素はマスタ/CSV/option mapの動的行であり、列集合が対象種別により変わるため固定shape化しない。既知propertyは契約し、追加列は該当サービス境界で扱う。 | 安定列はpropertiesへ昇格し、対象マスタ/CSV/外部カタログで増える列だけ互換境界として許容する。 |
| `var/json_schema/get-admin-master-data.json:rows.items` | dynamic-master-or-csv-row | 行 | 2 | 配列要素はマスタ/CSV/option mapの動的行であり、列集合が対象種別により変わるため固定shape化しない。既知propertyは契約し、追加列は該当サービス境界で扱う。 | 安定列はpropertiesへ昇格し、対象マスタ/CSV/外部カタログで増える列だけ互換境界として許容する。 |
| `var/json_schema/get-admin-order-status.json:orderStatuses.items` | status-summary-row | 注文ステータス | 5 | 配列要素はマスタ/CSV/option mapの動的行であり、列集合が対象種別により変わるため固定shape化しない。既知propertyは契約し、追加列は該当サービス境界で扱う。 | 安定列はpropertiesへ昇格し、対象マスタ/CSV/外部カタログで増える列だけ互換境界として許容する。 |
| `var/json_schema/get-admin-product-csv-category.json:columns.items` | dynamic-master-or-csv-row | CSV列 | 2 | 配列要素はマスタ/CSV/option mapの動的行であり、列集合が対象種別により変わるため固定shape化しない。既知propertyは契約し、追加列は該当サービス境界で扱う。 | 安定列はpropertiesへ昇格し、対象マスタ/CSV/外部カタログで増える列だけ互換境界として許容する。 |
| `var/json_schema/get-admin-product-csv-class-category.json:columns.items` | dynamic-master-or-csv-row | CSV列 | 2 | 配列要素はマスタ/CSV/option mapの動的行であり、列集合が対象種別により変わるため固定shape化しない。既知propertyは契約し、追加列は該当サービス境界で扱う。 | 安定列はpropertiesへ昇格し、対象マスタ/CSV/外部カタログで増える列だけ互換境界として許容する。 |
| `var/json_schema/get-admin-product-csv-class-name.json:columns.items` | dynamic-master-or-csv-row | CSV列 | 2 | 配列要素はマスタ/CSV/option mapの動的行であり、列集合が対象種別により変わるため固定shape化しない。既知propertyは契約し、追加列は該当サービス境界で扱う。 | 安定列はpropertiesへ昇格し、対象マスタ/CSV/外部カタログで増える列だけ互換境界として許容する。 |
| `var/json_schema/get-admin-product-csv-product.json:columns.items` | dynamic-master-or-csv-row | CSV列 | 2 | 配列要素はマスタ/CSV/option mapの動的行であり、列集合が対象種別により変わるため固定shape化しない。既知propertyは契約し、追加列は該当サービス境界で扱う。 | 安定列はpropertiesへ昇格し、対象マスタ/CSV/外部カタログで増える列だけ互換境界として許容する。 |
| `var/json_schema/get-admin-trade-law.json:tradeLawRows.items` | dynamic-master-or-csv-row | 特定商取引法表示行 | 7 | 配列要素はマスタ/CSV/option mapの動的行であり、列集合が対象種別により変わるため固定shape化しない。既知propertyは契約し、追加列は該当サービス境界で扱う。 | 安定列はpropertiesへ昇格し、対象マスタ/CSV/外部カタログで増える列だけ互換境界として許容する。 |
| `var/json_schema/post-admin-csv-config.json:columns.items` | dynamic-master-or-csv-row | CSV列 | 4 | 配列要素はマスタ/CSV/option mapの動的行であり、列集合が対象種別により変わるため固定shape化しない。既知propertyは契約し、追加列は該当サービス境界で扱う。 | 安定列はpropertiesへ昇格し、対象マスタ/CSV/外部カタログで増える列だけ互換境界として許容する。 |
| `var/json_schema/put-admin-master-data.json:rows.items` | dynamic-master-or-csv-row | 行 | 4 | 配列要素はマスタ/CSV/option mapの動的行であり、列集合が対象種別により変わるため固定shape化しない。既知propertyは契約し、追加列は該当サービス境界で扱う。 | 安定列はpropertiesへ昇格し、対象マスタ/CSV/外部カタログで増える列だけ互換境界として許容する。 |
| `var/json_validate/post-admin-csv-config.param.json:columns.items` | dynamic-master-or-csv-row | CSV列（入力） | 4 | 配列要素はマスタ/CSV/option mapの動的行であり、列集合が対象種別により変わるため固定shape化しない。既知propertyは契約し、追加列は該当サービス境界で扱う。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | 安定列はpropertiesへ昇格し、対象マスタ/CSV/外部カタログで増える列だけ互換境界として許容する。 |
| `var/json_validate/put-admin-master-data-edit.param.json:rows.items` | dynamic-master-or-csv-row | 行（入力） | 4 | 配列要素はマスタ/CSV/option mapの動的行であり、列集合が対象種別により変わるため固定shape化しない。既知propertyは契約し、追加列は該当サービス境界で扱う。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | 安定列はpropertiesへ昇格し、対象マスタ/CSV/外部カタログで増える列だけ互換境界として許容する。 |
| `var/json_validate/put-admin-order-status.param.json:orderStatuses.items` | status-summary-row | 注文ステータス（入力） | 3 | 配列要素はマスタ/CSV/option mapの動的行であり、列集合が対象種別により変わるため固定shape化しない。既知propertyは契約し、追加列は該当サービス境界で扱う。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | 安定列はpropertiesへ昇格し、対象マスタ/CSV/外部カタログで増える列だけ互換境界として許容する。 |
