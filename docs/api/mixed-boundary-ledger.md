# Mixed Boundary Ledger

string|integer|null は互換境界だけで許可します。token/key/tracking系やDB採番IDへ寄せられるものはschemaを狭めます。

| path | name | classification | reason | schema decision |
|---|---|---|---|---|
| `var/json_schema/delete-admin-calendar.json:calendarId` | `calendarId` | ec-cube-boundary | Fake文字列IDとEC-CUBE整数IDの両方が観察されるため、この境界だけmixedBoundaryIdとして扱う。 | EC-CUBE採番IDとFake文字列IDが同じResource境界に現れるため、この境界だけ互換IDとして扱う。 |
| `var/json_schema/delete-admin-payment-payment.json:paymentId` | `paymentId` | ec-cube-boundary | Fake文字列IDとEC-CUBE整数IDの両方が観察されるため、この境界だけmixedBoundaryIdとして扱う。 | EC-CUBE採番IDとFake文字列IDが同じResource境界に現れるため、この境界だけ互換IDとして扱う。 |
| `var/json_schema/get-admin-authority-role.json:authorityOptions.items.id` | `id` | ec-cube-boundary | Fake文字列IDとEC-CUBE整数IDの両方が観察されるため、この境界だけmixedBoundaryIdとして扱う。 | EC-CUBE採番IDとFake文字列IDが同じResource境界に現れるため、この境界だけ互換IDとして扱う。 |
| `var/json_schema/get-admin-calendar.json:calendars.items.id` | `id` | ec-cube-boundary | Fake文字列IDとEC-CUBE整数IDの両方が観察されるため、この境界だけmixedBoundaryIdとして扱う。 | EC-CUBE採番IDとFake文字列IDが同じResource境界に現れるため、この境界だけ互換IDとして扱う。 |
| `var/json_schema/get-admin-calendar.json:calendars.items.holiday` | `holiday` | ec-cube-boundary | EC-CUBE互換のフォーム/一覧境界で数値と文字列の両方が観察される。業務解釈はResource/Semantic層で行う。 | EC-CUBE採番IDとFake文字列IDが同じResource境界に現れるため、この境界だけ互換IDとして扱う。 |
| `var/json_schema/get-admin-csv-config.json:id` | `id` | ec-cube-boundary | Fake文字列IDとEC-CUBE整数IDの両方が観察されるため、この境界だけmixedBoundaryIdとして扱う。 | EC-CUBE採番IDとFake文字列IDが同じResource境界に現れるため、この境界だけ互換IDとして扱う。 |
| `var/json_schema/get-admin-mail-template.json:id` | `id` | ec-cube-boundary | Fake文字列IDとEC-CUBE整数IDの両方が観察されるため、この境界だけmixedBoundaryIdとして扱う。 | EC-CUBE採番IDとFake文字列IDが同じResource境界に現れるため、この境界だけ互換IDとして扱う。 |
| `var/json_schema/get-admin-mail-template.json:Mail.id` | `id` | ec-cube-boundary | Fake文字列IDとEC-CUBE整数IDの両方が観察されるため、この境界だけmixedBoundaryIdとして扱う。 | EC-CUBE採番IDとFake文字列IDが同じResource境界に現れるため、この境界だけ互換IDとして扱う。 |
| `var/json_schema/get-admin-master-data.json:rows.items.id` | `id` | ec-cube-boundary | Fake文字列IDとEC-CUBE整数IDの両方が観察されるため、この境界だけmixedBoundaryIdとして扱う。 | EC-CUBE採番IDとFake文字列IDが同じResource境界に現れるため、この境界だけ互換IDとして扱う。 |
| `var/json_schema/get-admin-order-status.json:orderStatuses.items.id` | `id` | ec-cube-boundary | Fake文字列IDとEC-CUBE整数IDの両方が観察されるため、この境界だけmixedBoundaryIdとして扱う。 | EC-CUBE採番IDとFake文字列IDが同じResource境界に現れるため、この境界だけ互換IDとして扱う。 |
| `var/json_schema/get-admin-payment-payment-list.json:payments.items.paymentId` | `paymentId` | ec-cube-boundary | Fake文字列IDとEC-CUBE整数IDの両方が観察されるため、この境界だけmixedBoundaryIdとして扱う。 | EC-CUBE採番IDとFake文字列IDが同じResource境界に現れるため、この境界だけ互換IDとして扱う。 |
| `var/json_schema/get-admin-payment-payment.json:paymentId` | `paymentId` | ec-cube-boundary | Fake文字列IDとEC-CUBE整数IDの両方が観察されるため、この境界だけmixedBoundaryIdとして扱う。 | EC-CUBE採番IDとFake文字列IDが同じResource境界に現れるため、この境界だけ互換IDとして扱う。 |
| `var/json_schema/get-admin-trade-law.json:tradeLawRows.items.id` | `id` | ec-cube-boundary | Fake文字列IDとEC-CUBE整数IDの両方が観察されるため、この境界だけmixedBoundaryIdとして扱う。 | EC-CUBE採番IDとFake文字列IDが同じResource境界に現れるため、この境界だけ互換IDとして扱う。 |
| `var/json_schema/get-products.json:filters.disp_number` | `disp_number` | ec-cube-boundary | EC-CUBE互換のフォーム/一覧境界で数値と文字列の両方が観察される。業務解釈はResource/Semantic層で行う。 | EC-CUBE採番IDとFake文字列IDが同じResource境界に現れるため、この境界だけ互換IDとして扱う。 |
| `var/json_schema/get-products.json:filters.pageno` | `pageno` | ec-cube-boundary | EC-CUBE互換のフォーム/一覧境界で数値と文字列の両方が観察される。業務解釈はResource/Semantic層で行う。 | EC-CUBE採番IDとFake文字列IDが同じResource境界に現れるため、この境界だけ互換IDとして扱う。 |
| `var/json_schema/get-products.json:products.items.id` | `id` | ec-cube-boundary | Fake文字列IDとEC-CUBE整数IDの両方が観察されるため、この境界だけmixedBoundaryIdとして扱う。 | EC-CUBE採番IDとFake文字列IDが同じResource境界に現れるため、この境界だけ互換IDとして扱う。 |
| `var/json_schema/post-admin-calendar.json:calendarId` | `calendarId` | ec-cube-boundary | Fake文字列IDとEC-CUBE整数IDの両方が観察されるため、この境界だけmixedBoundaryIdとして扱う。 | EC-CUBE採番IDとFake文字列IDが同じResource境界に現れるため、この境界だけ互換IDとして扱う。 |
| `var/json_schema/post-admin-calendar.json:holiday` | `holiday` | ec-cube-boundary | EC-CUBE互換のフォーム/一覧境界で数値と文字列の両方が観察される。業務解釈はResource/Semantic層で行う。 | EC-CUBE採番IDとFake文字列IDが同じResource境界に現れるため、この境界だけ互換IDとして扱う。 |
| `var/json_schema/post-admin-payment-payment-list.json:paymentId` | `paymentId` | ec-cube-boundary | Fake文字列IDとEC-CUBE整数IDの両方が観察されるため、この境界だけmixedBoundaryIdとして扱う。 | EC-CUBE採番IDとFake文字列IDが同じResource境界に現れるため、この境界だけ互換IDとして扱う。 |
| `var/json_schema/put-admin-payment-payment.json:paymentId` | `paymentId` | ec-cube-boundary | Fake文字列IDとEC-CUBE整数IDの両方が観察されるため、この境界だけmixedBoundaryIdとして扱う。 | EC-CUBE採番IDとFake文字列IDが同じResource境界に現れるため、この境界だけ互換IDとして扱う。 |
| `var/json_schema/put-admin-sort-no-move.json:rowId` | `rowId` | ec-cube-boundary | Fake文字列IDとEC-CUBE整数IDの両方が観察されるため、この境界だけmixedBoundaryIdとして扱う。 | EC-CUBE採番IDとFake文字列IDが同じResource境界に現れるため、この境界だけ互換IDとして扱う。 |
| `var/json_validate/delete-admin-calendar.param.json:calendarId` | `calendarId` | request-transport | カレンダーID（入力）は業務上IDだが、HTTPフォームでは文字列として届く。Resource/Semantic層の検証を通すためtransport schemaではstring\|integerを許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/delete-admin-mail-template.param.json:mailTemplateId` | `mailTemplateId` | request-transport | メールテンプレートID（入力）は業務上IDだが、HTTPフォームでは文字列として届く。Resource/Semantic層の検証を通すためtransport schemaではstring\|integerを許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/delete-admin-payment-payment.param.json:paymentId` | `paymentId` | request-transport | 支払方法ID（入力）は業務上IDだが、HTTPフォームでは文字列として届く。Resource/Semantic層の検証を通すためtransport schemaではstring\|integerを許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/get-admin-csv-config.param.json:id` | `id` | request-transport | ID（入力）は業務上IDだが、HTTPフォームでは文字列として届く。Resource/Semantic層の検証を通すためtransport schemaではstring\|integerを許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/get-admin-customer-delivery-edit.param.json:id` | `id` | request-transport | ID（入力）は業務上IDだが、HTTPフォームでは文字列として届く。Resource/Semantic層の検証を通すためtransport schemaではstring\|integerを許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/get-admin-customer.param.json:id` | `id` | request-transport | ID（入力）は業務上IDだが、HTTPフォームでは文字列として届く。Resource/Semantic層の検証を通すためtransport schemaではstring\|integerを許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/get-admin-payment-payment.param.json:paymentId` | `paymentId` | request-transport | 支払方法ID（入力）は業務上IDだが、HTTPフォームでは文字列として届く。Resource/Semantic層の検証を通すためtransport schemaではstring\|integerを許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/get-products.param.json:pageno` | `pageno` | request-transport | EC-CUBE互換のフォーム/一覧境界で数値と文字列の両方が観察される。業務解釈はResource/Semantic層で行う。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/get-products.param.json:disp_number` | `disp_number` | request-transport | EC-CUBE互換のフォーム/一覧境界で数値と文字列の両方が観察される。業務解釈はResource/Semantic層で行う。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/get-shopping-confirm.param.json:paymentMethodId` | `paymentMethodId` | request-transport | 支払方法IDは業務上IDだが、HTTPフォームでは文字列として届く。Resource/Semantic層の検証を通すためtransport schemaではstring\|integerを許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/post-admin-calendar.param.json:holiday` | `holiday` | request-transport | EC-CUBE互換のフォーム/一覧境界で数値と文字列の両方が観察される。業務解釈はResource/Semantic層で行う。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/post-admin-calendar.param.json:calendarId` | `calendarId` | request-transport | カレンダーID（入力）は業務上IDだが、HTTPフォームでは文字列として届く。Resource/Semantic層の検証を通すためtransport schemaではstring\|integerを許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/post-admin-mail-template.param.json:mailTemplateId` | `mailTemplateId` | request-transport | メールテンプレートID（入力）は業務上IDだが、HTTPフォームでは文字列として届く。Resource/Semantic層の検証を通すためtransport schemaではstring\|integerを許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/post-admin-order-create.param.json:paymentMethodId` | `paymentMethodId` | request-transport | 支払方法IDは業務上IDだが、HTTPフォームでは文字列として届く。Resource/Semantic層の検証を通すためtransport schemaではstring\|integerを許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/post-admin-order-create.param.json:orderItems.items.quantity` | `quantity` | request-transport | 数量（入力）は本来数値/列挙の業務値だが、HTTPフォームでは文字列として届く。Resource/Semantic層の400応答を奪わないためtransport schemaでは文字列入力を許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/post-admin-order-create.param.json:charge` | `charge` | request-transport | 手数料（入力）は本来数値/列挙の業務値だが、HTTPフォームでは文字列として届く。Resource/Semantic層の400応答を奪わないためtransport schemaでは文字列入力を許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/post-admin-payment-payment-list.param.json:charge` | `charge` | request-transport | 手数料（入力）は本来数値/列挙の業務値だが、HTTPフォームでは文字列として届く。Resource/Semantic層の400応答を奪わないためtransport schemaでは文字列入力を許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/post-admin-product-bulk-status.param.json:productStatus` | `productStatus` | request-transport | 商品ステータス（入力）は本来数値/列挙の業務値だが、HTTPフォームでは文字列として届く。Resource/Semantic層の400応答を奪わないためtransport schemaでは文字列入力を許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/post-admin-product.param.json:price02` | `price02` | request-transport | 販売価格（入力）は本来数値/列挙の業務値だが、HTTPフォームでは文字列として届く。Resource/Semantic層の400応答を奪わないためtransport schemaでは文字列入力を許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/post-admin-product.param.json:productStatus` | `productStatus` | request-transport | 商品ステータス（入力）は本来数値/列挙の業務値だが、HTTPフォームでは文字列として届く。Resource/Semantic層の400応答を奪わないためtransport schemaでは文字列入力を許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/post-cart-item.param.json:quantity` | `quantity` | request-transport | 数量（入力）は本来数値/列挙の業務値だが、HTTPフォームでは文字列として届く。Resource/Semantic層の400応答を奪わないためtransport schemaでは文字列入力を許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/post-shopping-shipping-multiple.param.json:allocations.items.shippingAddressId` | `shippingAddressId` | request-transport | 配送先ID（入力）は業務上IDだが、HTTPフォームでは文字列として届く。Resource/Semantic層の検証を通すためtransport schemaではstring\|integerを許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/post-shopping-shipping-multiple.param.json:allocations.items.quantity` | `quantity` | request-transport | 数量（入力）は本来数値/列挙の業務値だが、HTTPフォームでは文字列として届く。Resource/Semantic層の400応答を奪わないためtransport schemaでは文字列入力を許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/post-shopping-shipping.param.json:shippingAddressId` | `shippingAddressId` | request-transport | 配送先ID（入力）は業務上IDだが、HTTPフォームでは文字列として届く。Resource/Semantic層の検証を通すためtransport schemaではstring\|integerを許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/put-admin-order.param.json:charge` | `charge` | request-transport | 手数料（入力）は本来数値/列挙の業務値だが、HTTPフォームでは文字列として届く。Resource/Semantic層の400応答を奪わないためtransport schemaでは文字列入力を許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/put-admin-payment-payment.param.json:paymentId` | `paymentId` | request-transport | 支払方法ID（入力）は業務上IDだが、HTTPフォームでは文字列として届く。Resource/Semantic層の検証を通すためtransport schemaではstring\|integerを許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/put-admin-payment-payment.param.json:charge` | `charge` | request-transport | 手数料（入力）は本来数値/列挙の業務値だが、HTTPフォームでは文字列として届く。Resource/Semantic層の400応答を奪わないためtransport schemaでは文字列入力を許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/put-admin-product.param.json:price02` | `price02` | request-transport | 販売価格（入力）は本来数値/列挙の業務値だが、HTTPフォームでは文字列として届く。Resource/Semantic層の400応答を奪わないためtransport schemaでは文字列入力を許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/put-admin-product.param.json:productStatus` | `productStatus` | request-transport | 商品ステータス（入力）は本来数値/列挙の業務値だが、HTTPフォームでは文字列として届く。Resource/Semantic層の400応答を奪わないためtransport schemaでは文字列入力を許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/put-admin-sort-no-move.param.json:rowId` | `rowId` | request-transport | 行ID（入力）は業務上IDだが、HTTPフォームでは文字列として届く。Resource/Semantic層の検証を通すためtransport schemaではstring\|integerを許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/put-admin-toggle-visible.param.json:rowId` | `rowId` | request-transport | 行ID（入力）は業務上IDだが、HTTPフォームでは文字列として届く。Resource/Semantic層の検証を通すためtransport schemaではstring\|integerを許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
| `var/json_validate/put-cart-item.param.json:quantity` | `quantity` | request-transport | 数量（入力）は本来数値/列挙の業務値だが、HTTPフォームでは文字列として届く。Resource/Semantic層の400応答を奪わないためtransport schemaでは文字列入力を許容する。 Request schema is transport-level; business invalid values are allowed through to Resource/Semantic validation. | HTTP formでは文字列として届くため、Resource/Semantic層の400応答を奪わないtransport契約としてmixedを許可する。 |
