{#
  EC-CUBE default-theme Block/nav_sp.twig port.

  This is the hamburger affordance that opens the drawer menu on small
  screens. The drawer body itself is populated in base.html.twig with
  login_sp + category_nav_sp so it contains real navigable links.
#}
<div class="ec-headerNavSP">
    <i class="fas fa-bars"></i>
</div>
