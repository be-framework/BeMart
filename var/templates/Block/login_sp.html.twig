{#
  Block/login_sp.html.twig — mobile drawer quick-nav (anonymous state).

  Included by the storefront base layout inside the mobile drawer region.
  No extends. Self-contained.

  Links are derived from resource contracts:
    /cart    — Cart resource (goCart)
    /login   — Login resource (goLogin)
    /entry   — Customer registration (goCustomerRegistration)
    /        — Storefront index
#}
<nav class="idea-drawer-nav" aria-label="メニュー">
  <a class="idea-drawer-nav__item" href="/cart">
    <span class="idea-drawer-nav__label">カート</span>
    <span class="idea-eyebrow">Cart</span>
  </a>
  <a class="idea-drawer-nav__item" href="/login">
    <span class="idea-drawer-nav__label">ログイン</span>
    <span class="idea-eyebrow">Sign in</span>
  </a>
  <a class="idea-drawer-nav__item" href="/entry">
    <span class="idea-drawer-nav__label">新規登録</span>
    <span class="idea-eyebrow">Register</span>
  </a>
  <a class="idea-drawer-nav__item" href="/">
    <span class="idea-drawer-nav__label">ホーム</span>
    <span class="idea-eyebrow">Home</span>
  </a>
</nav>
