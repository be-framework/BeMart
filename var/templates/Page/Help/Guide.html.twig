{% extends 'IdeaStore/layout/base.html.twig' %}

{% block title %}ご利用ガイド | IDEA STORE{% endblock %}

{% block content %}
<nav class="idea-container idea-breadcrumb" aria-label="パンくずリスト">
  <a href="/">Home</a>
  <span aria-hidden="true">/</span>
  <span aria-current="page">ご利用ガイド</span>
</nav>

<section class="idea-container idea-section idea-section--tight" aria-labelledby="idea-guide-title">
  <header>
    <p class="idea-eyebrow">Shopping guide</p>
    <h1 id="idea-guide-title" class="idea-section-title">
      {{ staticContent.title|default('ご利用ガイド') }}
    </h1>
  </header>

  {% if staticContent.sections is defined and staticContent.sections|length > 0 %}
    <dl class="idea-spec-list" id="idea-guide-sections">
      {% for section in staticContent.sections %}
        <div class="idea-spec-row">
          {% if section.title is defined and section.title %}
            <dt>{{ section.title }}</dt>
          {% else %}
            <dt></dt>
          {% endif %}
          <dd>
            {% if section.body is defined and section.body %}
              {{ section.body|nl2br }}
            {% elseif section.content is defined and section.content %}
              {{ section.content|nl2br }}
            {% endif %}
          </dd>
        </div>
      {% endfor %}
    </dl>
  {% else %}
    <p class="idea-muted" id="idea-guide-empty">
      ショッピングガイドは準備中です。
    </p>
  {% endif %}

  <div class="idea-actions" style="margin-top:2rem;">
    <a class="idea-button idea-button--light" href="/" rel="goTop">トップへ戻る</a>
  </div>
</section>
{% endblock %}
