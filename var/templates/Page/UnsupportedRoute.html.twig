{% extends 'base.html.twig' %}

{% set route = routeName|default('unsupported_route') %}
{% set title = '未実装ルート' %}

{% block main %}
    <div class="ec-role">
        <div class="ec-pageHeader">
            <h1>未実装</h1>
        </div>
        <p>このルートはまだ BeMart 側に実装されていません。</p>
        <p><code>{{ routeName|default('') }}</code></p>
    </div>
{% endblock %}
