{#
  BeMart admin カスタマイズJavaScript編集 — EC-CUBE goJs.

  PORT of EC-CUBE 4.3 admin-theme `Content/js.twig`
  (src/Eccube/Resource/template/admin/Content/js.twig).

  FORM/CRUD page. Extends the admin frame (`admin-base.html.twig`). The
  markup skeleton + `c-*` / `ec-*` / Bootstrap classes are kept verbatim
  from EC-CUBE; only the dynamic bindings are rebound. This is the
  clone-from-one twin of `Css.html.twig` (EC-CUBE's css.twig / js.twig
  share an identical structure).

  FORM-PAGE recipe (Ray.WebFormModule — see var/templates/README.md): the
  Js resource (src/Resource/Page/Admin/Content/Js.php — a thin HTML
  renderer, see its port header) exposes an
  {@see \MyVendor\BeMart\Form\AdminJsForm} as `body['form']`. The JS
  textarea renders through that real form object:
    {{ form.input('js')|raw }}      <- real <textarea> w/ id `form_js`
  `AdminJsForm::init()` ports the field name / id from EC-CUBE's inline
  `JsController` form (block prefix `form`).

  EC-CUBE-ism rebinding (see var/templates/README.md, "Admin pages"):
   - `{{ 'key'|trans }}`  -> the ja literal from messages.ja.yaml.
   - `url(route)`         -> deterministic `/{route}` (BeMartTwigExtension).
   - `form_widget(form._token)` -> EC-CUBE per-request CSRF widget. The
     hidden `_token` input is kept (structure) with an empty value;
     EC-CUBE-runtime only — enumerated as residual.
   - EC-CUBE uses ACE for the visible editor and a hidden textarea for the
     submitted value. BeMart does not ship ACE in the port, so the real
     Ray.WebFormModule textarea is shown directly and populated from
     `public/assets/js/customize.js`.
#}
{% extends 'admin-base.html.twig' %}

{% set route = 'admin_content_js' %}
{% set title = 'JavaScript管理' %}
{% set sub_title = 'コンテンツ管理' %}

{% block stylesheet %}
    <style>
        .ui-resizable-se {
            right: -3px;
            bottom: -3px;
        }
    </style>
{% endblock %}

{% block main %}
    <form id="content_js_form" method="post" action="{{ url('admin_content_js') }}">
        <input type="hidden" id="form__token" name="_token" value="{{ csrfToken|default(csrf_token('bemart')) }}">

        <div class="c-contentsArea__cols">
            <div class="c-contentsArea__primaryCol">
                <div class="c-primaryCol">
                    <!-- ページ設定 -->
                    <div class="card rounded border-0 mb-4">
                        <div class="card-header">
                            <div class="row">
                                <div class="col-8"><span class="card-title">JavaScript設定</span></div>
                                <div class="col-4 text-end">
                                    <a data-bs-toggle="collapse" href="#pageDetail" aria-expanded="false" aria-controls="pageDetail">
                                        <i class="fa fa-angle-up fa-lg"></i>
                                    </a>
                                </div>
                            </div>
                        </div>
                        <div class="collapse show ec-cardCollapse" id="pageDetail">
                            <div class="card-body">
                                <!-- コード -->
                                <div class="row mb-2">
                                    <div class="col-2">
                                        <div class="d-inline-block" data-bs-toggle="tooltip" data-bs-placement="top" title="カスタマイズ用JavaScriptファイルを編集します。JavaScriptで記述します。">
                                            <span>コード</span><i class="fa fa-question-circle fa-lg ms-1"></i>
                                        </div>
                                        <span class="badge bg-primary ms-1">必須</span>
                                    </div>
                                    <div class="col-10">
                                        {{ form.input('js')|raw }}
                                        {% if form.error('js') %}<span class="invalid-feedback d-block">{{ form.error('js') }}</span>{% endif %}
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="c-conversionArea">
            <div class="c-conversionArea__container">
                <div class="row justify-content-between align-items-center">
                    <div class="col-6">
                        <div class="c-conversionArea__leftBlockItem">
                            <a class="c-baseLink" href="{{ url('admin_content_page') }}">
                                <i class="fa fa-backward" aria-hidden="true"></i>
                                <span>JavaScript管理</span>
                            </a>
                        </div>
                    </div>
                    <div class="col-6">
                        <div id="ex-conversion-action" class="row align-items-center justify-content-end">
                            <div class="col-auto">
                                <button id="save-button" class="btn btn-ec-conversion px-5" type="submit">登録</button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </form>
{% endblock %}
