{% extends 'admin-base.html.twig' %}

{% set route = routeName|default('admin-unsupported-route') %}
{% set title = '利用できないルート' %}

{% block main %}
<div class="idea-admin-content">
    <h2 class="idea-admin-content__title">利用できないルート</h2>
    <div class="idea-admin-section">
        <p class="idea-admin-notice">この管理画面ルートはまだ BeMart 側に実装されていません。</p>
        <dl class="idea-admin-dl">
            <dt>route</dt>
            <dd><code>{{ routeName|default('') }}</code></dd>
        </dl>
    </div>
</div>
{% endblock %}
